services:
  bd:
    image: postgres:16
    environment:
      POSTGRES_USER: luisg
      POSTGRES_PASSWORD: 123
      POSTGRES_DB: runtime
    ports:
      - "5432:5432"
    volumes:
      - postgres-runtime:/var/lib/postgresql/data

volumes:
  postgres-runtime:
