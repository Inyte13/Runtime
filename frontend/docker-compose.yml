name: runtime
services:
  frontend:
    build: .
    ports:
      - "${HOST_PORT}:${DOCKER_PORT}"
    volumes:
      - .:/frontend
      - frontend-modules:/frontend/node_modules
    env_file:
      - .env

volumes:
  frontend-modules:
